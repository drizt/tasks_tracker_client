# Use LF for all text files on every platform.
* text=auto eol=lf

# Windows files that require native line endings.
*.bat text eol=crlf
*.rc text eol=crlf

# Binary files present in the repository.
*.ico binary
*.jar binary
*.png binary
